{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "DBConfig": {
    "ConnectionString": "${DB_CONNECTION_STRING}"
  },
  "bcrypt": {
    "salt": "${BCRYPT_SALT}"
  }
}
